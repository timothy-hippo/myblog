<% extend('./layouts/layout.ejs')%>
<div class="blog">
  <nav class="pl-0 mb-3">
    <a class="breadcrumb-item text-primary" href="/">Home</a>
    <% for(item in categories){ %> <%
    if(article.category===categories[item].id){ %>
    <a class="breadcrumb-item text-primary" href="/<%- categories[item].path %>"
      ><%- categories[item].name %></a
    >
    <% } %> <% } %>

    <span class="breadcrumb-item active"><%- article.title %></span>
  </nav>
  <div class="">
    <div class="">
      <!-- artical header -->
      <h1 class="text-primary"><%- article.title %></h1>
      <div class="d-flex flex-row text-secondary small">
        <div class="mr-2">
          <%- moment(article.update_time*1000).format("YYYY/MM/DD") %>
        </div>
        <div>
          <i class="fa fa-tag" aria-hidden="true"></i>
          <% for(item in categories){ %> <%
          if(article.category===categories[item].id){ %>
          <a class="text-info" href="#"><%- categories[item].name %></a>
          <% } %> <% } %>
        </div>
      </div>
      <article class="text-dark post mb-4">
        <p><%- article.content || '' %></p>
      </article>
      <!-- 留言 -->
      <div class="card mb-3">
        <div class="card-body">
          <div id="disqus_thread"></div>
          <script>
            /**
             *  RECOMMENDED CONFIGURATION VARIABLES: EDIT AND UNCOMMENT THE SECTION BELOW TO INSERT DYNAMIC VALUES FROM YOUR PLATFORM OR CMS.
             *  LEARN WHY DEFINING THESE VARIABLES IS IMPORTANT: https://disqus.com/admin/universalcode/#configuration-variables    */
            /*
    var disqus_config = function () {
    this.page.url = PAGE_URL;  // Replace PAGE_URL with your page's canonical URL variable
    this.page.identifier = PAGE_IDENTIFIER; // Replace PAGE_IDENTIFIER with your page's unique identifier variable
    };
    */
            (function () {
              // DON'T EDIT BELOW THIS LINE
              var d = document,
                s = d.createElement("script");
              s.src = "https://blog-note.disqus.com/embed.js";
              s.setAttribute("data-timestamp", +new Date());
              (d.head || d.body).appendChild(s);
            })();
          </script>
          <noscript
            >Please enable JavaScript to view the
            <a href="https://disqus.com/?ref_noscript"
              >comments powered by Disqus.</a
            ></noscript
          >
        </div>
      </div>
    </div>
  </div>
</div>
